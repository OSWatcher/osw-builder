# osw-builder

> OSWatcher tool to build and capture a whole set of images

## Setup

~~~
git submodule update --init
~~~

## Usage

Open `config.yaml` and configure the images order, sources, names, etc..

Then you can build them
~~~
$ ./builder.py config.yaml
~~~
