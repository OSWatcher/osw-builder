Vagrant.configure("2") do |config|
    config.vm.box = "{box_name}"
    config.vm.define "{box_name}"

    config.vm.provider :libvirt do |libvirt|
        libvirt.default_prefix = "{box_name}"
        # PLACEHOLDER
    end
end
